# Python
__pycache__/
*.py[cod]
*.pyd
*.pyo

# Virtual environment
venv/
.venv/
env/

# Data (raw cache can be huge — don't commit it)
data/raw/

# Trained models — committed so Streamlit Cloud can use them at startup
# models/*.pkl  ← intentionally NOT ignored (file is < 1 MB)

# Environment secrets
.env

# Jupyter
.ipynb_checkpoints/
*.ipynb

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
